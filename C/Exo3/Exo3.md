## Exercice 3

On considère une suite définie par

```bash
U(n+1) = (U(n))^2 + U(n) + 1 et U(o) = 1
```

On se donne un seuil noté s, calculez la plus petit valeur de n telle que

```bash
U(n) >= s
```

Cette exercice peut se voir comme une autre modifications de l'exercice 2.