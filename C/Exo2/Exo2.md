## Exercice 2

On considère une suite définie par

```bash
U(n+1) = a x U(n) + b et U(o) = 1.
```
Connaissant a, b et N, on cherche à calculer

```bash
U(N) et (Somme des U(n) allant de n = 0 à N)
```